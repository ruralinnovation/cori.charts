% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{add_logo_svg}
\alias{add_logo_svg}
\title{Add CORI logo to an SVG chart without rasterizing}
\usage{
add_logo_svg(plot_path, logo_path, logo_position, logo_scale, logo_margin)
}
\arguments{
\item{plot_path}{file path to the chart/plot/graphic. Must be a \code{.png} or
\code{.svg} file; other file types raise an error.}

\item{logo_path}{Path to the logo. Defaults to hosted Full CORI Black logo}

\item{logo_position}{Combination of top/bottom and right/left. Defaults to top right.}

\item{logo_scale}{Scale logo to 1/10 width of plot}

\item{logo_margin}{Gap between the logo and the two borders it sits
nearest, as a fraction of the plot's dimensions: a single number for
both axes, or \code{c(x, y)} to set them separately (e.g. \code{0.05} for 5\%, or
\code{c(0.04, 0.06)}). Defaults to \code{NULL}, which keeps the padding this
function has always used.}
}
\value{
magick image (a rasterized preview of the updated file; the
persisted file at \code{plot_path} remains fully vector)
}
\description{
Internal helper used by \code{\link[=add_logo]{add_logo()}} when \code{plot_path} is an
SVG file. Overlays the logo as a nested \verb{<svg>} element inside the plot's
root \verb{<svg>} element (the standard technique for embedding one vector
document inside another), so the chart's vector paths and text are
preserved rather than rasterized or traced. Always writes the result back
to \code{plot_path}, since there is no way to produce a true vector SVG output
via any raster round trip.
}
\keyword{internal}
