% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{inline_logo_styles}
\alias{inline_logo_styles}
\title{Resolve a logo's own styling onto its shape elements}
\usage{
inline_logo_styles(logo_root)
}
\arguments{
\item{logo_root}{the logo document's root \verb{<svg>} node}
}
\value{
\code{logo_root}, modified in place
}
\description{
Internal helper used by \code{\link[=add_logo_svg]{add_logo_svg()}}. CSS in the host
document reaches into an embedded \verb{<svg>}: an svglite chart carries
\verb{.svglite path \{ fill: none; stroke: #000000 \}} on its root, which
outranks a logo's own \verb{.st0 \{ fill: ... \}} rule (specificity 0-1-1 beats
0-1-0) and also outranks any \code{fill="..."} presentation attribute, leaving
the logo a hollow black outline. An inline \code{style} attribute outranks
every author rule whatever its selector, so each shape's resolved fill and
stroke are written inline before the logo is embedded.
}
\keyword{internal}
